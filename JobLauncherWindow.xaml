<Window x:Class="Booking.JobLauncherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Launch Booking Jobs"
        Height="560"
        Width="720"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Job Launcher" FontSize="24" FontWeight="SemiBold" />

        <materialDesign:Card Grid.Row="1" Padding="14" Margin="0,10,0,10">
            <StackPanel>
                <TextBlock Text="Booking Profile" FontWeight="SemiBold" />
                <ComboBox Margin="0,8,0,0"
                          ItemsSource="{Binding Profiles}"
                          SelectedItem="{Binding SelectedProfile}"
                          DisplayMemberPath="ProfileName" />
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" Padding="14">
            <StackPanel>
                <TextBlock Text="Select Accounts" FontWeight="SemiBold" Margin="0,0,0,10" />
                <ListBox ItemsSource="{Binding AccountSelections}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <CheckBox IsChecked="{Binding IsSelected}" />
                                <TextBlock Grid.Column="1" Margin="8,0,0,0" Text="{Binding Account.Username}" />
                                <TextBlock Grid.Column="2" Text="{Binding Account.SessionState}" Opacity="0.8" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </materialDesign:Card>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Margin="0,0,8,0" Command="{Binding CancelCommand}" />
            <Button Content="Launch Jobs"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding LaunchCommand}" />
        </StackPanel>
    </Grid>
</Window>
